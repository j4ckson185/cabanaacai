import React, { useState, useEffect } from 'react';
import { ShoppingCart, Search, X } from 'lucide-react';

const Menu = () => {
  const [products, setProducts] = useState([]);
  const [categories, setCategories] = useState([
    'Todos',
    'Apenas Cremes',
    'Açaí com Creme de Ninho',
    'Açaí com Creme de Ovomaltine',
    'Açaí com Creme de Amendoim',
    'Açaí puro',
    'Monte seu Açaí'
  ]);
  const [selectedCategory, setSelectedCategory] = useState('Todos');
  const [cartItems, setCartItems] = useState([]);
  const [searchQuery, setSearchQuery] = useState('');
  const [selectedProduct, setSelectedProduct] = useState(null);
  const [showCart, setShowCart] = useState(false);

  // Mock products data
  useEffect(() => {
    setProducts([
      {
        id: 1,
        name: 'Açaí com Ninho',
        description: 'Açaí na tigela com creme de leite ninho',
        price: 18.90,
        category: 'Açaí com Creme de Ninho',
        image: '/api/placeholder/100/100',
        toppings: ['Granola', 'Banana', 'Morango', 'Leite em pó'],
        minToppings: 1,
        maxToppings: 3
      },
      // More products would be added here
    ]);
  }, []);

  const Header = () => (
    <div className="flex items-center justify-between p-4 bg-purple-700 text-white">
      <img src="/api/placeholder/40/40" alt="Logo" className="w-10 h-10" />
      <h1 className="text-2xl font-bold">Cabana Açaí</h1>
      <div className="relative">
        <ShoppingCart size={24} />
        {cartItems.length > 0 && (
          <span className="absolute -top-2 -right-2 bg-red-500 text-white rounded-full w-5 h-5 flex items-center justify-center text-xs">
            {cartItems.length}
          </span>
        )}
      </div>
    </div>
  );

  const Categories = () => (
    <div className="overflow-x-auto p-4">
      <div className="flex space-x-4">
        {categories.map((category) => (
          <button
            key={category}
            onClick={() => setSelectedCategory(category)}
            className={`px-4 py-2 rounded-full whitespace-nowrap ${
              selectedCategory === category
                ? 'bg-purple-700 text-white'
                : 'bg-gray-200'
            }`}
          >
            {category}
          </button>
        ))}
      </div>
    </div>
  );

  const SearchBar = () => (
    <div className="p-4">
      <div className="relative">
        <Search className="absolute left-3 top-3 text-gray-400" size={20} />
        <input
          type="text"
          placeholder="O que você quer comer hoje?"
          className="w-full p-2 pl-10 border rounded-lg"
          value={searchQuery}
          onChange={(e) => setSearchQuery(e.target.value)}
        />
      </div>
    </div>
  );

  const ProductCard = ({ product }) => (
    <div
      onClick={() => setSelectedProduct(product)}
      className="flex p-4 border rounded-lg cursor-pointer hover:shadow-lg"
    >
      <div className="flex-1">
        <h3 className="font-bold">{product.name}</h3>
        <p className="text-sm text-gray-600">{product.description}</p>
        <p className="mt-2 font-bold text-purple-700">
          R$ {product.price.toFixed(2)}
        </p>
      </div>
      <img
        src={product.image}
        alt={product.name}
        className="w-20 h-20 object-cover rounded-lg"
      />
    </div>
  );

  const ProductModal = () => {
    const [selectedToppings, setSelectedToppings] = useState([]);

    if (!selectedProduct) return null;

    return (
      <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4">
        <div className="bg-white rounded-lg max-w-lg w-full p-6">
          <div className="flex justify-between items-start">
            <h2 className="text-xl font-bold">{selectedProduct.name}</h2>
            <button onClick={() => setSelectedProduct(null)}>
              <X size={24} />
            </button>
          </div>
          <img
            src={selectedProduct.image}
            alt={selectedProduct.name}
            className="w-full h-48 object-cover rounded-lg my-4"
          />
          <p className="text-gray-600 mb-4">{selectedProduct.description}</p>
          <p className="font-bold text-purple-700 mb-4">
            R$ {selectedProduct.price.toFixed(2)}
          </p>
          
          <div className="mb-4">
            <h3 className="font-bold mb-2">
              Acompanhamentos (min: {selectedProduct.minToppings}, max:{' '}
              {selectedProduct.maxToppings})
            </h3>
            <div className="space-y-2">
              {selectedProduct.toppings.map((topping) => (
                <label key={topping} className="flex items-center">
                  <input
                    type="checkbox"
                    checked={selectedToppings.includes(topping)}
                    onChange={() => {
                      if (selectedToppings.includes(topping)) {
                        setSelectedToppings(
                          selectedToppings.filter((t) => t !== topping)
                        );
                      } else if (
                        selectedToppings.length < selectedProduct.maxToppings
                      ) {
                        setSelectedToppings([...selectedToppings, topping]);
                      }
                    }}
                    className="mr-2"
                  />
                  {topping}
                </label>
              ))}
            </div>
          </div>

          <button
            onClick={() => {
              if (
                selectedToppings.length >= selectedProduct.minToppings &&
                selectedToppings.length <= selectedProduct.maxToppings
              ) {
                setCartItems([
                  ...cartItems,
                  { ...selectedProduct, selectedToppings },
                ]);
                setShowCart(true);
                setSelectedProduct(null);
              }
            }}
            disabled={
              selectedToppings.length < selectedProduct.minToppings ||
              selectedToppings.length > selectedProduct.maxToppings
            }
            className="w-full bg-purple-700 text-white py-2 rounded-lg disabled:bg-gray-400"
          >
            Adicionar ao Carrinho
          </button>
        </div>
      </div>
    );
  };

  const CartModal = () => {
    if (!showCart) return null;

    const total = cartItems.reduce((sum, item) => sum + item.price, 0);

    return (
      <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4">
        <div className="bg-white rounded-lg max-w-lg w-full p-6">
          <div className="flex justify-between items-start mb-4">
            <h2 className="text-xl font-bold">Seu Pedido</h2>
            <button onClick={() => setShowCart(false)}>
              <X size={24} />
            </button>
          </div>

          <div className="space-y-4 mb-4">
            {cartItems.map((item, index) => (
              <div key={index} className="border-b pb-2">
                <h3 className="font-bold">{item.name}</h3>
                <p className="text-sm text-gray-600">
                  Acompanhamentos: {item.selectedToppings.join(', ')}
                </p>
                <p className="font-bold text-purple-700">
                  R$ {item.price.toFixed(2)}
                </p>
              </div>
            ))}
          </div>

          <p className="font-bold text-lg mb-4">
            Total: R$ {total.toFixed(2)}
          </p>

          <div className="flex space-x-4">
            <button
              onClick={() => setShowCart(false)}
              className="flex-1 bg-gray-200 py-2 rounded-lg"
            >
              Continuar Comprando
            </button>
            <button
              onClick={() => {
                // Handle order completion
                setShowCart(false);
                setCartItems([]);
              }}
              className="flex-1 bg-purple-700 text-white py-2 rounded-lg"
            >
              Finalizar Pedido
            </button>
          </div>
        </div>
      </div>
    );
  };

  const filteredProducts = products.filter(
    (product) =>
      (selectedCategory === 'Todos' ||
        product.category === selectedCategory) &&
      (!searchQuery ||
        product.name.toLowerCase().includes(searchQuery.toLowerCase()) ||
        product.description.toLowerCase().includes(searchQuery.toLowerCase()))
  );

  return (
    <div className="min-h-screen bg-gray-50">
      <Header />
      <Categories />
      <SearchBar />
      
      <div className="p-4 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
        {filteredProducts.map((product) => (
          <ProductCard key={product.id} product={product} />
        ))}
      </div>

      <ProductModal />
      <CartModal />
    </div>
  );
};

export default Menu;
